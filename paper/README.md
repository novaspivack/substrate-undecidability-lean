# Paper — Substrate Undecidability

**Title:** *Discrete or Continuous? Why No Internal Observer Can Decide, and What Can Adjudicate Instead*

Source: [`Substrate_Undecidability.tex`](Substrate_Undecidability.tex) · Bibliography: [`refs.bib`](refs.bib) · Compiled PDF: [`Substrate_Undecidability.pdf`](Substrate_Undecidability.pdf)

## Build

```bash
cd paper
pdflatex -interaction=nonstopmode Substrate_Undecidability.tex
bibtex Substrate_Undecidability
pdflatex -interaction=nonstopmode Substrate_Undecidability.tex
pdflatex -interaction=nonstopmode Substrate_Undecidability.tex
```

Requires a standard TeX Live installation (`natbib`, `cleveref`, `tcolorbox`, `stmaryrd`, `booktabs`, `longtable`). Bibliography style: `plainnat` (numeric citations via `natbib`, author/year available via `\citeyear`).

## Status

First full draft complete. `Substrate/*.lean` builds cleanly (`lake build`: 8262/8262 jobs) with zero `sorry` and zero custom axioms; Appendix A's `#print axioms` column reflects the real, final `lake env lean scripts/axiom_audit.lean` output (all named theorems depend on at most the three standard Mathlib axioms `propext`, `Classical.choice`, `Quot.sound`, with one lemma depending on none). See [`specs/INCOMPLETE/EPIC_003_SUD_PAPER/`](../specs/INCOMPLETE/EPIC_003_SUD_PAPER/) for the epic tracker.

A domain-expert mathematical-correctness review pass has been completed and folded in: the halting-oracle escape claim for Theorem 4 was corrected (a `0''` oracle escapes it; a plain `0'` halting oracle is not shown to, since the Lean reduction establishes only co-halting-hardness, a lower bound, not `LawMatches`'s exact position in the arithmetical hierarchy), axis B′ (efficient computability / complexity class, as distinct from axis B's computability question) was introduced and the Aaronson/NP-hard evidence correctly reattributed to it throughout, Theorem 3's application to hypercomputation-demonstration scaling was restated in terms of the actual density argument (Theorem 1) rather than an informal "resource-hiding artifact" framing, the glimmer corollary's Born-martingale lemma was corrected to target computable randomness rather than Martin-Löf randomness (matching what the stated argument actually establishes), the four-class taxonomy's exhaustiveness was tied explicitly to Paper 51's bound, and Lemma 2.2's proof sketch now states explicitly that the discrete/continuum state-type equivalence is obtained non-constructively via `Cardinal.eq`.

Two further open problems have since been closed in Lean (`Substrate/Cardinality.lean`, `Substrate/Probabilistic.lean`) and folded into the paper: the cardinality/uncountable-observer strengthening of Theorem 5 is now a proved Corollary (`Substrate.no_joint_substrate_decision`, holding for *any* index type, including uncountable ones), and the deterministic half of the probabilistic-observers question is now proved (`Substrate.acceptance_on_D_iff_Cstrict`: no internal test has exclusive class advantage). The error-tolerant, measure-theoretic generalization of the latter remains open, honestly scoped in §10 to the precise missing piece (Mathlib's `Measure.pi` is finite-index-only; the separately-available Ionescu–Tulcea-based `Measure.infinitePi` has not yet been wired into this repository's Baire-space setting). The remaining open problems (computable-analysis refinement, quantum version, Born-martingale lemma) were tightened to cite the precise missing Mathlib machinery, verified independently by direct source search (zero occurrences of `Weihrauch`/`CauchyModulus`, zero POVM/CPTP formalizations, zero Martin-Löf/Schnorr randomness or computable-martingale predicates, as of this writing).

A subsequent Lean pass wired `Measure.infinitePi` directly onto Baire space (`Substrate/Probabilistic.lean`, commit `b7555f1`) and proved a genuine new result — `Substrate.D_null_geometric`/`Substrate.Cstrict_full_mass_geometric`: under the canonical geometric i.i.d. product measure, `D` has measure zero and `Cstrict` has full measure — now stated as **Corollary 5.2** ("Measure-theoretic typicality of the continuum class") immediately after the cardinality Corollary 5.1. That same Lean pass also *briefly* claimed the full ε-tolerant generalization itself was closed via a `ProbabilisticTest`/`HasProbAcceptAbove` construction; on review this was caught as an overclaim (the construction is prefix-determined with no internal randomness, so it transports *identical*, not merely ε-close, confidence values between classes — the tolerance parameter does no real work) and was honestly retracted back to open by the Lean agent itself. The paper never carried the overclaim into a committed state, but §10's probabilistic-observers bullet and Appendix B's status row have been tightened into an explicit three-way split (deterministic case: `A_LEAN`; measure-zero/one classification: `A_LEAN`; ε-tolerant *randomized* generalization: **open**, with the precise reason prefix-transport does not bound aggregate class-conditional acceptance rates), and `HasProbAcceptAbove` is omitted from Appendix A as a notational variant carrying no independent content. A whole-document grep for "ε-tolerant" and "probabilistic" confirmed no other passage implies the retracted closure.

A citation-attribution review pass (against an already-accurate `refs.bib`) fixed four prose-level misattributions: the area/holographic entropy bound was reattributed from Bekenstein (whose 1981 result is an energy–radius bound, not an area bound) to 't Hooft (1993) and Susskind (1995), both newly added to `refs.bib`; Hossenfelder & Palmer (2020) is now correctly cited as the standing challenge to the no-superdeterminism assumption, not as support for it; the Bombelli–Henson–Sorkin causal-set result is now described as ruling out a preferred Lorentz frame, not discreteness signatures altogether (causal sets still exhibit their own, frame-invariant discreteness signatures); and the Pour-El–Richards wave-equation result now carries its regularity caveat (the non-computable solution arises from non-\(C^1\) computable initial data).

A further, independently-discovered pre-existing defect was fixed while doing the above: the paper's theorem/lemma/corollary environments were auto-numbered by a single LaTeX counter shared with (and reset per-section alongside) definitions and remarks, causing every displayed theorem-box number (e.g. "Lemma 5.1", "Theorem 5.7", "Theorem 8.2/8.4") to silently disagree with the hand-referenced numbering used everywhere else in the prose ("Lemma 2.1", "Theorem 4", "Theorem 5"). Lemma/Theorem/Corollary now have independent counters (with a handful of explicit `\setcounter`/`\addtocounter` adjustments at the points where the informal scheme is irregular — Corollary N.1 attached to its parent theorem, and Theorem 5 spanning two separate `theorem` environments for its outsourcing and MDL-uniqueness halves) so that every auto-generated number and every `\cref` pointer now agrees exactly with the paper's own cross-references; Definition/Remark numbering (never in conflict) is unchanged.

A subsequent polish pass did a full adversarial re-read of the entire prose (not only recently-touched sections) and a LaTeX-layout cleanup. Content fixes: the abstract and conclusion's MDL-uniqueness sentence now flags explicitly that the uniqueness argument is cited from the companion NEMS programme rather than re-derived here (the Introduction already said this; the abstract/conclusion did not, which a hostile reader could mistake for a Lean-checked claim); Table 1's caption now states explicitly that axes A and B are shown undecidable only *taken together*, not separately in isolation, matching the Introduction's own careful framing; and the Bekenstein-bound formula now carries its Boltzmann constant (\(k_B\)), which had been dropped. LaTeX-layout fixes, verified by an actual before/after count of `pdflatex`'s Overfull/Underfull warnings (70 → 6, the remaining six all mild body-text underfulls with badness ≤ 4765, none visually detectable): (i) Table 1 (the axes/evidence table, §3) had drifted to the very last page, after the bibliography — its `[h]` placement specifier had no fallback, and once it failed to fit "here" it queued indefinitely with nothing to force it out until `\end{document}`; fixed by loading `placeins` and adding a `\FloatBarrier` at the end of §3, plus widening/rebalancing its columns and switching them to `\raggedright` (matching the appendix tables' existing style) to kill ~45 underfull warnings from justifying narrow, citation-heavy cells; the glimmer four-class-taxonomy table got the same `\raggedright` fix. (ii) Corollary 5.2's Lean-citation parenthetical rendered with ugly, wide inter-word gaps from full justification on a very short trailing line (badness 10000) — the same construction (a short `\emph{(\leanref{...}, module ...)}` line immediately following a display equation, at the very end of a theorem/lemma/corollary environment) was found and fixed identically in five other places (Lemma 2.1, Theorem 2, Theorem 3, Theorem 4, Corollary 5.1), each now scoped `\raggedright` rather than document-wide. (iii) The `\leanref` macro allowed line breaks at `_`, `.`, and camelCase boundaries but not at `/` (module-path separators), causing real (not merely cosmetic) overfull hboxes wherever a long `Substrate/Foo.lean`-style reference or a literal `\(\D\)/\(\mathtt{Cstrict}\)` pairing had to wrap in a narrow table column; fixed at the macro level (one fix, all occurrences) plus added breakable spaces around the remaining literal-slash pairings in Appendix A/B. (iv) A long math display in Corollary 5.1's statement was overflowing the margin; split across two lines with `multline*`. Two further pre-existing issues were found and fixed along the way, unrelated to the above: the manual `\setcounter{corollary}{0}`/`\addtocounter{theorem}{-1}` counter resets used since the numbering fix (see above) were causing hyperref to assign the *same* PDF destination name to multiple distinct theorem-like environments (confirmed via `pdflatex`'s "destination with the same identifier" warnings), meaning some `\cref` hyperlinks could silently navigate to the wrong target even though the displayed number was correct; fixed by redefining `\theHtheorem`/`\theHcorollary` to prefix the anchor with `\thepage` (hyperref's own documented remedy for a counter reset after first use), which fully disambiguates every colliding pair since each falls on a different page. Two `\subsection` headings containing math (`B\('\)`) or a `\cref` were producing PDF-bookmark-string warnings; fixed with `\texorpdfstring`.

A paper-revision round 2 pass (per [`SPEC_001_PR2_SUD`](../specs/INCOMPLETE/EPIC_005_SUD_PAPER_REVISION_2/SPEC_001_PR2_SUD_PAPER_REVISION_ROUND_2.md)) landed the non-Lean-dependent items in full and the Lean-dependent items with honest, clearly-flagged pending status. Landed in full: a new §1.1 "Perfect Self-Containment, for physicists" grounding PSC as a criterion (not a metaphysical bet) for a physics audience, with the self-consistency argument for its axiomatic status, citing nine new external-literature keys (Guth, Freivogel, Wheeler, Einstein, Everett, Hartle–Hawking, Rovelli, Rissanen, Solomonoff) each independently verified against a citation database before being added; a corrected `refs.bib` DOI table (13 new `SpivackPxx`/`SpivackOnRamp`/`SpivackUGP51` keys, every concept DOI independently re-verified against the live Zenodo API in this pass, not reused unverified from any prior draft); NEMS Paper 35 ("Oracles as External Selectors") cited in §2.5 and near Theorem 3 as the programme's own internal axis-B "no internal hypercomputer" result, distinct from the external-literature axis-B′ citations already present; a new §8 paragraph clarifying that "discrete" in this paper's classes is a description-length notion ("finitely generated"), not a geometry ("lattice"), with the Universal Generative Principle's Φ_MDL/GTE framework as the worked example of a continuum substrate with a finite generator (D ∩ C by transcript); a companion paragraph reconciling NEMS Paper 24's discrete-seed claim with this paper's substrate-agnosticism via the generative-priority-not-temporal-priority distinction (Infinity Compression's bare-certificate/enriched-realization/comparison-map structure), citing Papers 18/24/27; and a `SpivackP5[0-9]`/`SpivackUGP` grep confirming no other NEMS/UGP paper-number citation collision exists in the finished revision.

The same pass promoted the glimmer corollary (§10.1) out of "companion-paper material" framing into a load-bearing part of Theorem 5's own statement — Theorem 5's conclusion and the abstract now say "finite-description class (D ∪ Glimmer)" rather than "finite-information class," since D alone would contradict NEMS Paper 12's determinism no-go result — and rewrote the "proposed derivation chain" to the corrected four-step form (non-degeneracy premise from Paper 12 → Born-martingale lemma, open, needing Papers 13/14's completeness premise → UGP Paper 51's degree-exactly-0′ bound → hence glimmer), with Paper 11 demoted to context-only rather than doing the class-1-exclusion work, plus a new one-sentence finite-lifetime/prefix-incompressibility idealization caveat. This pass also drafted (but does **not** yet claim as machine-checked) a companion Lean module, `Substrate/Glimmer.lean` (definitions, density facts, and the bet-everything martingale construction) and a companion axis-A module, `Substrate/AxisA.lean` (the `Dfin` eventually-periodic pair and its own Theorem-2 instance) — as of this writing, neither module is imported by the root `Substrate.lean`, neither is part of the audited, sorry-free build, and both currently fail to compile standalone (a concurrent Lean revision pass was actively reconciling call-site signatures across `NotFinDet.lean`/`Glimmer.lean`/`AxisA.lean` at the time this paper-side pass concluded). The paper cites every intended Lean declaration name from both modules precisely, with an explicit, visible "PENDING, not `A_LEAN`" disclosure at each citation site (§8's axis-A remark, §10.1's Lean-status box, Appendix B's status table) rather than silently shipping the claim as machine-checked or silently omitting the citation — matching this repository's established honest-placeholder convention. One item did land cleanly in Lean during this same window and is now correctly credited: `Substrate.not_finitely_determined_of_dense_pair` (the abstract dense-pair form of Theorem 2, brief item B.4), which is sorry-free, part of the green build, and confirmed by direct `#print axioms` to depend on only `propext, Classical.choice, Quot.sound` with no `Nat.Partrec`/`Computable` dependence in its proof term — cited in Theorem 2's keybox and added to Appendix A.

The abstract and §6 item 2 ("what the theorems do not say") were both updated for consistency with the glimmer promotion: axis C is no longer described as wholly "silent" or deferred to a companion paper, since the classification itself (modulo the pending Lean build) is no longer an open problem — only the Born-martingale lemma's physical completeness premise remains genuinely open, and this is now stated precisely rather than folded into a blanket silence claim.

A follow-up pass closed out the one item the previous pass had correctly left as an honest placeholder: `Substrate/Glimmer.lean` and `Substrate/AxisA.lean` are now both genuinely green (`lake build`: 8262/8262 jobs, up from 8260; zero `sorry` tree-wide; commits `e81bfcb` for AxisA and `a2dae80`/`b86c553`/`0dcdcef` for Glimmer, the last of which fixed `glimmer_finite_description` from a vacuous `∃ _e : Code, True` statement to its real, non-vacuous content), independently re-verified by this pass via a direct `lake build` and a fresh `lake env lean scripts/axiom_audit.lean` run (plus a direct `#print axioms` sweep over every declaration not covered by that script) rather than trusting the report. Every PENDING/"not `A_LEAN`" disclosure added in the previous pass — the axis-A remark (§3), the glimmer status quote box (§10.1), and Appendix B's two status rows — was flipped to `A_LEAN`, fixing along the way a naming error the fresh Lean read caught: `Substrate.Succeeds`, `Substrate.ComputablyRandom`, and `Substrate.Glimmer` are top-level `Substrate.*` declarations, not nested under `Substrate.BornMeasure.*` as the previous pass's citations (drafted before the module was finished) had assumed. The Born-martingale lemma's own status is unchanged and correctly stays **Open** — only the surrounding definitional and separation machinery around it closed; the lemma's physical completeness premise (that the actual Born measure constitutes complete internal semantics) remains a physical/interpretive claim no formalization can close on its own. Appendix A's job count and axiom table were refreshed (8260→8262, with new rows for every Glimmer/AxisA theorem cited in the main text, each independently re-audited by this pass), and the Π⁰₂-hardness-of-`LawMatches` open problem (§10) was tightened from a bare "not carried out here" to the precise technical obstruction the Lean side actually found: `LawBarrier.lean`'s `Tot`/`matchFnTot`/`bigGTot` scaffold shows the reduction is blocked because no step-bounded approximation of the totality predicate can match `LawMatches` uniformly across all time steps for total codes (slow-but-total inputs are indistinguishable from non-halting for arbitrarily many early stages) — a genuinely attempted and blocked result, not an unattempted one. Recompiled end-to-end (`pdflatex`→`bibtex`→`pdflatex`×2) with zero errors and zero undefined references.

A final pass closed the last open item of `SPEC_001_PR2_SUD`: `law_match_pi2_hard` (Theorem 4′) is now genuinely proved in Lean (commit `f5835c2`), independently re-verified by this pass via a direct `lake build` (8262/8262 jobs, unchanged — a proof addition, not a new dependency) and a fresh `#print axioms` run on `law_match_pi2_hard`/`law_match_pi2_hard_zero` (only `propext`, `Classical.choice`, `Quot.sound`). The result is a genuine many-one reduction `Tot ≤ₘ LawMatches f`, via a *different* construction from the one previously described as blocked: not step-bounded totality-approximation dualization, but sequential composition — run the candidate code to completion on each input (unbounded, not step-bounded), discard the result, and emit `f`. Since `Tot` is Π⁰₂-complete (cited as an external, standard recursion-theory fact, not proved in this repository), this establishes `LawMatches f` is Π⁰₂-hard, strictly above the co-halting-hardness lower bound of the original Theorem 4. Three edits followed: the "Π⁰₂-hardness of `LawMatches`" open-problems bullet was removed entirely (closed, not open); the two places stating the reduction shows "only co-halting-hardness, not Π⁰₂-hardness proper" (the introduction's "why does this matter" paragraph and §6 item 5) were upgraded to state Π⁰₂-hardness is now proved, making the "0″ escapes Theorem 4, no oracle escapes Theorem 2" framing exact (a plain 0′ oracle *provably* does not escape, since no Π⁰₂-complete problem is 0′-decidable) rather than a one-sided lower bound; and Appendix A gained rows for `law_match_pi2_hard`/`law_match_pi2_hard_zero` with their axiom-audit entries (job count stays 8262, unchanged — a proof addition, not a new heavy dependency).

The same pass sharpened, without changing its status, the Born-martingale lemma's own "Open" disclosure (§10.1's derivation chain and Appendix B's status row): the gap is not a randomness-theory gap (the martingale/computable-randomness machinery is already fully built and machine-checked in `Glimmer.lean`); the naive route of defining "complete internal semantics" as "the realized transcript is computably random" is foreclosed by this repository's no-smuggling convention, and would in any case be building in the conclusion, since the unrestricted universal form of that premise is outright false here (`BetMartingale.bet_succeeds` beats every *computable* non-degenerate `f`); and `nems-lean`'s `BICS` predicate (Papers 13/14), the nearest existing candidate for "complete internal semantics," is a quantum semantic-architecture predicate over density operators with no notion of a realized sequence or martingale at all — categorically the wrong kind of object for a direct bridge. What remains open is exactly and only a non-question-begging formalization of the physical premise as a predicate on realized transcripts.

A final full-paper coherence read-through (not a new adversarial pass, just a consistency check across all revision rounds) caught three pre-existing stale spots that predated this session's Lean closures and had been missed in earlier passes: the abstract's closing sentence still said glimmer's Lean module was "still being finished" and that "the martingale separation completing the classification" was left open (both now closed); the Conclusion still said the results were "silent on objective randomness" (no longer true since the glimmer corollary's definitions and separations are machine-checked); and §9's "what is proved versus what is cited" boundary list omitted the by-then-closed glimmer, axis-A, and Theorem 4′ results entirely. All three were fixed in place. Recompiled end-to-end (`pdflatex`→`bibtex`→`pdflatex`×2) with zero errors, zero undefined references or citations, zero overfull boxes; 34 pages, unchanged.

This completes `SPEC_001_PR2_SUD`'s full implementation: every item in its addendum §1–§5 deliverable list is now either done (prose and Lean both) or explicitly, precisely disclosed as open (the Born-martingale lemma's physical completeness premise; the ε-tolerant randomized generalization of the probabilistic-observers result; the question of which substrate actually obtains).

A dedicated final QA pass (full, careful, first-time-reader read-through of all ~1735 lines, not a quick coherence check) followed the `law_match_pi2_hard` closure above. Independently re-verified ground truth rather than trusting prior claims: re-ran `lake build` (8262 jobs), re-ran `lake env lean scripts/axiom_audit.lean` plus a direct `#print axioms` sweep on every Appendix A row not covered by that script (all confirmed accurate, including `finitely_determined_compl`'s claimed zero-axiom status), and mechanically checked every one of the 81 distinct `\leanref{}` identifiers cited anywhere in the paper body against the built library (zero unresolved identifiers) and every citation key against `refs.bib` (zero unresolved citations; 7 bib entries present but never cited, noted as informational, not fixed since dead-but-correct entries are not a correctness bug). Traced the theorem/lemma/corollary counter logic by hand across every `\setcounter`/`\addtocounter` in the file to confirm no numbering drift from any prior round's edits (Lemma 2.1–2.2, Theorem 1–5, Corollary 1.1/4.1/5.1/5.2 all confirmed correct against the compiled PDF's actual rendered numbers). Two small, real inconsistencies found and fixed: the Appendix A module map's `Substrate/LawBarrier.lean` row still described the module as containing only Theorem 4, omitting the newly-closed Theorem 4′; and Appendix B's status table listed "Theorems 1–4, Corollary 4.1" without naming Corollary 1.1 (the disjointness/density corollary), which has its own status but was only implicitly covered by the surrounding umbrella row. Everything else checked — abstract-to-body consistency, section flow and integration of every major addition from this revision round (PSC §1.1, glimmer §10.1, axis-A remark, P24/P48 reconciliation, SpivackP35 citations, Born-martingale sharpened disclosure), terminology consistency (finite-information vs. finite-description, Glimmer/glimmer capitalization, no stray `D_fin`), and Open Problems' bullet list — was already correct from prior rounds; no further changes needed. Recompiled clean, 34 pages (unchanged), zero errors/undefined refs/citations/overfull boxes.

A prose-quality pass fixed two real problems a human reader caught in the compiled PDF that all prior "coherence check" and "full read-through" passes had missed, because those passes checked technical correctness rather than reading flow. First, the Introduction jumped straight from the three-axis (A/B/C) bullet list into §1.1's dense, multi-paragraph PSC treatment with no transition: no sentence stated what the paper actually shows, and PSC appeared with no motivation for why a physics audience needed several paragraphs of it. Fixed by inserting two new bridging paragraphs immediately before the `\subsection{Perfect Self-Containment, for physicists}` heading: one stating the paper's core claim in a sentence or two (internal undecidability of axes A/B via two independent, Lean-machine-checked barriers), and one motivating PSC as the standing assumption Part II (not Part I) depends on, before handing off to §1.1's detailed grounding. Second, §10.1 (the glimmer corollary) and several other spots read like a project devlog rather than a finished paper — sentences that only make sense as responses to a prior framing ("Glimmer is not companion-paper material," "\(\D\) alone [...] in an earlier draft of Theorem 5's consequence," "Contrary to an earlier assessment," "Unlike an earlier version of this argument") or that narrated the paper's own revision history ("is no longer wholly deferred," "is now closed," "is now exactly one thing," "is no longer an obstacle," "was never a physical open problem [...] and that engineering effort is now complete"). A full-paper grep for this pattern found roughly a dozen instances beyond the two the user quoted, spanning §6 ("what the theorems do not say"), §9 ("honest limitations" and "forward-looking directions"), §10 (open problems), §10.1 (glimmer), and the Conclusion. Every instance was rewritten to state its content directly as settled fact — preserving all of the underlying mathematical and physical reasoning (why \(\D\) alone would be the wrong class, why Glimmer resolves it, why the Born-martingale gap is narrow rather than generic) while removing every reference to what an earlier draft said, planned, or assessed. Recompiled end-to-end (`pdflatex`→`bibtex`→`pdflatex`×2) with zero errors, zero undefined references or citations; 35 pages (up from 34, from the new bridging paragraphs).

A second expert-domain review pass fixed one substantive mathematical-framing problem and one structural inconsistency, plus several smaller items. The substantive fix: Corollary 5.2 ("Measure-theoretic typicality of the continuum class") previously invited the reading that discreteness is measure-zero-typical under *any* canonical sampling measure — false, since the Solomonoff/MDL universal prior (already cited in §1.1 as MDL's formal version) assigns *positive* mass to every computable sequence, favoring `D`, not `Cstrict`. The corollary's statement was narrowed to say precisely what its Lean proof establishes (`D_null_geometric`/`Cstrict_full_mass_geometric`: under the geometric i.i.d. product measure(s) specifically), and its discussion paragraph was rewritten to state the Solomonoff contrast explicitly (citing Li & Vitányi's textbook, newly added to `refs.bib`) and draw the punchline the corollary was previously missing: the disagreement between a geometric-measure adjudicator and a universal-prior adjudicator lives entirely in the choice of measure, and choosing a sampling measure over Baire space is itself the external-selector move §1.1 already identifies as PSC's target (the same failure mode as eternal inflation's measure problem) — which is exactly why Theorem 5's actual adjudicator routes through MDL rather than through any sampling measure. The structural fix: §10.1 "The glimmer corollary" — settled, load-bearing content that Theorem 5's conclusion depends on — was moved out of §10 "Open Problems and Extensions" (where it had contradicted its own settled status) into §8 as new §8.1, immediately after Corollary 5.2; only the genuinely open Born-martingale lemma remains under §10, as a short pointer back to §8.1 rather than a duplicate of the full taxonomy. The Introduction's axis-C sentence and Structure paragraph were updated to match (glimmer's classification is settled Part II content; only the Born-martingale premise is open).

Smaller fixes in the same pass: Theorem 4′ (Π⁰₂-hardness of `LawMatches`) gained a real `\newtheorem*` environment (previously prose-only, referenced but never formally stated) placed after Theorem 4; the two-environment `\addtocounter{theorem}{-1}` trick that had given Theorem 5's outsourcing and MDL-uniqueness halves duplicate hyperref-colliding "Theorem 5" identities was replaced with one merged `\begin{theorem}` environment with labeled parts (a) and (b); "Paper 51's bound establishes `f ≤_T 0′` unconditionally" was corrected to "under UGP P51's hypotheses (the DSAC model)" (its actual, conditional scope); a stale post-§1.1 PSC reintroduction ("the standing assumption... that a self-contained physical system has no genuine access to anything outside itself" — the old cosmos-hypothesis-flavored phrasing §1.1 was written specifically to replace) was cut down to "the criterion of §1.1"; Theorem 3's relation to NEMS Paper 35 was corrected from "reaches the same conclusion as" to "complements" (Theorem 3 is a certifier-silence result, Paper 35 is an oracle-exclusion result — related, not identical); and the abstract's glimmer description was trimmed from technical jargon ("limit-computable and computably random relative to the law," "martingale separation") to a single physicist-readable clause. Verified against Lean source rather than assumed: Corollary 5.2's proof-sketch claim that "the per-coordinate geometric measure of any single symbol is bounded above by `p`" was checked against Mathlib's `geometricPMFReal p n = (1-p)^n * p` (a distribution over all of `ℕ`, maximized at `n=0` giving exactly `p`) and confirmed correct as stated — the alternative bound `max(p, 1-p)` applies to a binary Bernoulli-style geometric parameterization, not to this repository's `ℕ`-valued one, so no change was needed there. Recompiled end-to-end (`pdflatex`→`bibtex`→`pdflatex`×3) with zero errors, zero undefined references or citations, zero multiply-defined labels; 36 pages (up from 35, from the new Theorem 4′ environment and the glimmer section's expanded standalone placement).

Theorem numbering, Lean references, and the epistemic status table (Appendix B) follow [`SPEC_000_SUD` §9](../specs/INCOMPLETE/EPIC_000_SUD_BOOTSTRAP/SPEC_000_SUD_SUBSTRATE_UNDECIDABILITY.md).

## Zenodo DOI injection

This paper follows the same `nova-publishing` Zenodo-DOI scaffolding used by the other Nova Spivack Lean-paper repos (e.g. `representational-incompleteness-lean`, `reflexive-architecture-lean`): [`nova_zenodo_doi_placeholder.tex`](nova_zenodo_doi_placeholder.tex) defines `\NovaZenodoDOI` (and `\NovaZenodoHubDOI`), `Substrate_Undecidability.tex` `\input{}`s it right after `\documentclass`, and prints the archival-DOI line via `\NovaZenodoAfterTitle` (after `\maketitle`) and `\NovaZenodoInAbstract` (end of the abstract). The DOI value is currently empty (`\def\NovaZenodoDOI{}`), which renders as "*Archival version (Zenodo): [DOI pending publication]*" — this paper has **not** been deposited to Zenodo yet.

Once this paper is actually deposited to Zenodo and registered in the `nova-publishing` registry (`artifact_id: lean-substrate-undecidability`), inject the real DOI with:

```bash
cd /Users/nova/nova-publishing
python3 -m nova_publishing.cli doi inject --artifact lean-substrate-undecidability --apply --target tex --allow-unpublished-registry
```

then verify the injected value is the **concept DOI** (`SELECT doi_concept FROM artifacts WHERE artifact_id='lean-substrate-undecidability'` in `data/registry.sqlite`), not the version DOI that `doi inject` writes by default — see `docs/ZENODO_DOI_POLICY.md` §6–7 in `nova-publishing`. If it wrote the version DOI, replace it manually in `nova_zenodo_doi_placeholder.tex` (between the `NOVA_ZPO_DOI_BEGIN`/`END` markers) and recompile with `latexmk -pdf -g -f -interaction=nonstopmode`.
